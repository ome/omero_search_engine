Check the status of an asynchronous query
---
tags:
 -  Check job query status
parameters:
  - name: query_id
    in: query
    type: string
    required: true

responses:
  200:
    description: A JSON contains resource attributes
    examples:
      resource:
        - Gene In Validation Screen"
        - GO Molecular Function"
    schema:
      $ref: '#/definitions/resource'
